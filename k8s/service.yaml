apiVersion: v1
kind: Service
metadata:
  name: debt-stalker
  namespace: debt-stalker
  labels:
    app: debt-stalker
    component: web
spec:
  type: ClusterIP
  selector:
    app: debt-stalker
    component: web
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
      name: http
