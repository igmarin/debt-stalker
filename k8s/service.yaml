apiVersion: v1
kind: Service
metadata:
  name: debt-stalker
  namespace: debt-stalker
  labels:
    app: debt-stalker
spec:
  type: ClusterIP
  selector:
    app: debt-stalker
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
      name: http
